/**
 * 📥 ingest skeleton
 * ─────────────────────────────
 * まだ何も処理しないプレースホルダ。
 * 将来ここに:
 *   1. RSS 取得
 *   2. 差分抽出
 *   3. GPT で shortTitle 生成
 *   4. news.json 更新
 * を実装していく。
 */
console.log("📥 ingest skeleton – nothing happens yet");
